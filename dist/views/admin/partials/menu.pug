ul.menu
  li
    a(class="btn btn-primary" href=`/${admin}/dashboard`) Tổng Quan

  if (myAccount.permissions.includes("groupTopicView"))
    li
      a(class="btn btn-primary" href=`/${admin}/groupTopics`) Chủ Đề Cộng Đồng
  
  if (myAccount.permissions.includes("roomChatView"))
    li
      a(class="btn btn-primary" href=`/${admin}/roomChats`) Phòng Trò Chuyện

  if (myAccount.permissions.includes("userView"))
    li
      a(class="btn btn-primary" href=`/${admin}/users`) Người Dùng

  if (myAccount.permissions.includes("accountView"))
    li
      a(class="btn btn-primary" href=`/${admin}/accounts`) Tài Khoản

  if (myAccount.permissions.includes("roleView")) 
    li
      a(class="btn btn-primary" href=`/${admin}/roles`) Vai Trò
      
  if (myAccount.permissions.includes("roleView")) 
    li
      a(class="btn btn-primary" href=`/${admin}/permissions`) Phân Quyền