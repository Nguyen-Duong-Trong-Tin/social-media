ul.menu
  li
    a(class="btn btn-secondary" href=`/${admin}/dashboard`) Tổng Quan
  hr

  if (myAccount.permissions.includes("groupView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/groups`) Cộng Đồng
  if (myAccount.permissions.includes("groupTopicView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/groupTopics`) Chủ Đề Cộng Đồng
  if (myAccount.permissions.includes("articleGroupView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/articleGroups`) Bài Viết Cộng Đồng
  if (myAccount.permissions.includes("taskGroupView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/taskGroups`) Nhiệm Vụ Cộng Đồng
  if (myAccount.permissions.includes("taskGroupSubmissionView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/taskGroupSubmissions`) Nộp Bài Nhiệm Vụ Cộng Đồng
  hr

  if (myAccount.permissions.includes("userView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/users`) Người Dùng
  if (myAccount.permissions.includes("articleUserView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/articleUsers`) Bài Viết Người Dùng
  hr

  if (myAccount.permissions.includes("roomChatView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/roomChats`) Phòng Trò Chuyện
  hr

  if (myAccount.permissions.includes("accountView"))
    li
      a(class="btn btn-secondary" href=`/${admin}/accounts`) Tài Khoản

  if (myAccount.permissions.includes("roleView")) 
    li
      a(class="btn btn-secondary" href=`/${admin}/roles`) Vai Trò
      
  if (myAccount.permissions.includes("roleView")) 
    li
      a(class="btn btn-secondary" href=`/${admin}/permissions`) Phân Quyền